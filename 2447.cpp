#include <iostream>
using namespace std;

void star(int n, int x, int y) {
    if(n=1)
}
int N[6563][6563];
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n; cin >> n;
    star(n,n-1,n-1);

}
